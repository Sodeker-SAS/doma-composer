<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::connection((string) config('attachments.connection', 'tenant'))
            ->create((string) config('attachments.table', 'attachments'), function (Blueprint $table): void {
                $table->id();
                $table->char('uuid', 26)->unique();
                $table->string('name');
                $table->string('storage_location', 120)->nullable();
                $table->text('url');
                $table->string('extension', 20);
                $table->bigInteger('size');
                $table->char('status', 1)->default('1');
                $table->char('created_by', 26);
                $table->char('updated_by', 26);
                $table->timestamps();
                $table->softDeletes();
            });
    }

    public function down(): void
    {
        Schema::connection((string) config('attachments.connection', 'tenant'))
            ->dropIfExists((string) config('attachments.table', 'attachments'));
    }
};
